import React from 'react';

import Button from './Button';

const QuickFilterButton: React.FC = () => null;
// <Button className={css} show={show} onClick={onClick}>
//   + {title}
// </Button>

export default QuickFilterButton;
